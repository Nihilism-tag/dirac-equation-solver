# 狄拉克方程数值求解器工程说明

## 项目概述

本项目实现了一套用于求解含屏蔽库仑势的单电子径向狄拉克方程的数值计算框架。主要包含三种数值方法：**B-样条法（B-spline）**、**映射代数基伪谱法（MAB-GPS）**以及基于MAB-GPS的**稳定化方法（Stabilization Method）**，用于在正能连续区中识别共振态。

### 核心物理问题

求解以下径向狄拉克方程：

$$
\hat{H} = \begin{pmatrix} V(r) + mc^2 & c(-\frac{d}{dr} + \frac{\kappa}{r}) \\ c(\frac{d}{dr} + \frac{\kappa}{r}) & V(r) - mc^2 \end{pmatrix}
$$

其中：
- **势能项**：$V(r) = -\frac{Z}{r}e^{-\lambda r}$（带屏蔽的库仑势）
- **核电荷数**：$Z=92$（类铀体系）
- **屏蔽参数**：$\lambda=0.1$
- **量子数**：$\kappa=1$（对应$p_{1/2}$态，为共振搜索提供离心势垒）

## 代码文件结构

### 核心模块

| 文件名 | 功能描述 | 关键策略 |
|--------|----------|----------|
| `config.py` | 物理常数与数值参数配置 | 原子单位制、MAB-GPS映射参数 |
| `potential.py` | 势能函数与解析解公式 | 屏蔽库仑势、狄拉克能量解析公式 |
| `bspline.py` | B-样条有限元求解器 | 局部基底、对比基准 |
| `mabgps.py` | MAB-GPS伪谱求解器 | **CGL节点+边界截断+非对称求解** |
| `stab_scan.py` | 稳定化扫描模块 | L尺度扫描、能级跟踪、共振识别 |
| `stab_plot.py` | 可视化模块 | 稳定化图、共振波函数与有效势 |
| `run_stab.py` | 稳定化方法入口 | 三阶段流程编排 |
| `test_solvers.py` | 核心求解器测试 | 6项测试（含波函数平滑性验证） |
| `test_stabilization.py` | 稳定化方法测试 | 4类10项测试 |
| `main.py` | 主程序入口 | MAB-GPS与B-样条对比 |
| `plot.py` | 基础绘图功能 | 能谱图、波函数对比图 |

### 生成文件

- `fig_stabilization.png` — 稳定化图（共振平台可视化）
- `fig_resonance_wf.png` — 共振波函数与有效势
- `spectrum.png` — MAB-GPS与B-样条能谱对比
- `wavefunction.png` — 波函数对比图

## 数值方法策略与原理

### 1. MAB-GPS方法（核心求解器）

#### 1.1 节点选择策略：切比雪夫-高斯-洛巴托（CGL）节点

**选择理由**：
- **原代码缺陷**：使用Legendre-Gauss节点（开区间$(-1,1)$），无法覆盖原点$r=0$
- **CGL优势**：包含端点$x=-1$和$x=+1$，经映射$r(x) = L\frac{1+x}{1-x+\alpha}$后可精确到达物理边界
- **解析公式**：$x_k = -\cos\left(\frac{k\pi}{N-1}\right)$，$k=0,...,N-1$

**关键代码**：
```python
def _cgl_nodes(N):
    k = np.arange(N)
    return -np.cos(np.pi * k / (N - 1))  # 包含±1端点
```

#### 1.2 微分矩阵构建：Trefethen谱微分公式

**原理**：基于重心拉格朗日插值的谱微分矩阵

$$
D_{ij} = \frac{c_i}{c_j} \frac{(-1)^{i+j}}{x_i - x_j} \quad (i \neq j)
$$

**对角元约束**（关键稳定性保障）：
$$
D_{ii} = -\sum_{j \neq i} D_{ij}
$$

其中$c_k=2$（端点）或$1$（内部）。该求和规则确保微分算子的守恒性。

**选择理由**：
- 谱精度：指数收敛而非多项式收敛
- 边界聚集：CGL节点在边界处密集，适合解析边界层行为

#### 1.3 边界条件实施：矩阵截断法

**策略**：提取内部节点（排除首尾端点）

```python
interior_idx = np.arange(1, N - 1)  # 索引1到N-2
r_interior = r_all[interior_idx]   # 不含r=0和r=r_max
D_r_interior = D_r_all[np.ix_(interior_idx, interior_idx)]
```

**物理意义**：
- 强制$P(0)=Q(0)=0$（原点正则性）
- 强制$P(r_{max})=Q(r_{max})=0$（边界硬壁）
- 结果：$r_{min} = 1.06 \times 10^{-4}$ a.u.（避开奇点）

#### 1.4 哈密顿矩阵求解：非对称实部提取法

**历史演进**：

| 阶段 | 策略 | 问题 | 当前方案 |
|------|------|------|----------|
| 原始版 | `eig`+虚部过滤 | 非厄密、40%特征值丢弃 | ❌ 废弃 |
| 修复版 | 对称化+`eigh` | 奇偶解耦、Nyquist振荡 | ❌ 废弃 |
| **当前版** | `eig`+`.real` | 保持谱精度、无虚部过滤 | ✅ 采用 |

**当前策略原理**：
```python
eigenvalues_complex, eigenvectors = linalg.eig(H)
eigenvalues = eigenvalues_complex.real  # 虚部~1e-14数值噪声
```

**选择理由**：
1. **保持谱精度**：避免对称化导致的中央差分退化
2. **无奇偶解耦**：Nyquist模式($+1,-1,+1,-1...$)不会出现
3. **波函数平滑**：验证通过`oscillation_rate < 0.5`测试
4. **全谱保留**：296个特征值全部保留（$2 \times (150-2)$）

### 2. 稳定化方法（共振态搜索）

#### 2.1 三阶段流程

```
Phase 1: L尺度扫描 (L ∈ [0.5, 3.0], step=0.05)
    ↓ 收集能隙内本征值
Phase 2: 能级跟踪与共振识别
    ↓ 计算dE/dL，寻找平台
Phase 3: 可视化输出
```

#### 2.2 L尺度扫描原理

**物理机制**：改变映射参数$L$相当于改变计算"盒子"的大小
- 连续态能量：随$L$增加而下降（$-1/L^2$趋势）
- 共振态能量：几乎不随$L$变化（平台特征）

**能量窗口**：$[0.9mc^2, 1.2mc^2]$（正能连续区）

#### 2.3 能级跟踪算法

**贪心最近邻匹配**：
```python
# 对相邻L步的能量本征值进行匹配
for each new_eigenvalue:
    find nearest unassigned track within threshold (0.5 * mc2 * L_step)
    if matched: assign to existing track
    else: start new track
```

**NaN填充策略**：处理变长本征值集合，保证轨迹矩阵规整

#### 2.4 共振识别准则

**平台判据**：在中心60%的$L$范围内，$|dE/dL|$方差最小

```python
# 评分函数
score = var(|dE/dL|) over central 60% range
resonance = argmin(score)
```

**结果示例**（实际运行）：
- 共振能量：$E_{res} = 0.999892 \, mc^2$（18776.84 a.u.）
- 最优尺度：$L_{opt} = 1.0000$

### 3. B-样条法（对比基准）

**功能**：提供未加动能平衡处理的有限元参照

**特性**：
- 局部基底、稀疏矩阵
- 在强库仑场下发生**变分崩溃**
- 能谱退化为堆积在$\pm mc^2$边界的非物理连续态
- 作为MAB-GPS成功重构的对比基准

## 关键参数配置

### 物理常数（原子单位制）

| 参数 | 值 | 说明 |
|------|-----|------|
| `C_LIGHT` | 137.036 | 光速（a.u.） |
| `M_ELECTRON` | 1.0 | 电子质量 |
| `Z` | 92 | 核电荷（铀） |
| `LAMBDA_SHIELD` | 0.1 | 屏蔽参数 |
| `KAPPA` | 1 | $p_{1/2}$态量子数 |

### MAB-GPS映射参数

| 参数 | 值 | 物理意义 |
|------|-----|----------|
| `ALPHA_MAP` | 0.1 | 控制原点附近网格密度 |
| `L_SCALE` | 1.0 | 径向映射尺度 |
| `N_POINTS` | 150 | CGL节点总数（含端点） |
| 内部节点 | 148 | $N-2$，实际用于组装H |
| $r_{max}$ | 20 a.u. | $2L/\alpha$，波函数衰减域 |

## 验证测试结果

### 核心求解器测试（test_solvers.py）

| 测试项 | 状态 | 关键指标 |
|--------|------|----------|
| 势能单调性 | ✅ PASS | $V(r)$随$r$递增（负值减小） |
| 原点安全性 | ✅ PASS | $V(0)$有限 |
| 氢原子解析解 | ✅ PASS | 误差<1e-4 |
| MAB-GPS无虚假态 | ✅ PASS | 296=2×(150-2) |
| MAB-GPS精度验证 | ✅ PASS | 基态误差<1e-4 |
| **波函数平滑性** | ✅ PASS | 振荡率<0.5 |
| B-样条虚假态 | ✅ PASS | 确认存在（预期行为） |

### 稳定化方法测试（test_stabilization.py）

| 类别 | 测试数 | 覆盖场景 |
|------|--------|----------|
| 扫描结构 | 3 | 字典结构、有限性、空窗口处理 |
| 能级跟踪 | 2 | 理想平台、空集合处理 |
| 共振查找 | 2 | 平台检测、None返回 |
| 绘图功能 | 2 | 无头绘制、None输入处理 |
| **合计** | **9** | 全部PASS |

### 物理验证指标

| 指标 | 目标值 | 实际值 | 状态 |
|------|--------|--------|------|
| 基态能量误差 | < 1e-6 | 3.29e-08 | ✅ |
| $P(r_{max})$ | < 1e-4 | 6.40e-06 | ✅ |
| 本征值数量 | 296 | 296 | ✅ |
| $r_{min}$ | > 0 | 1.06e-04 | ✅ |
| 束缚态数量 | < 50 | 28 | ✅ |

## 重要技术决策记录

### 决策1：放弃对称化，采用`eig+.real`

**背景**：H_issue.md发现对称化`H=0.5*(H+H.T)`导致奇偶解耦（Nyquist振荡）

**分析**：
- 对称化将全局谱算子退化为局部中央差分
- Nyquist模式（$+1,-1,+1,-1...$）的导数为零，形成"零动能陷阱"
- 求解器利用此陷阱叠加高频噪声以降低总能量

**决策**：移除对称化，改用`scipy.linalg.eig`+`.real`提取

**验证**：波函数平滑性测试`oscillation_rate=0.163 < 0.5` ✅

### 决策2：CGL节点+矩阵截断实施边界条件

**对比方案**：
- Plan A（采用）：CGL节点+截断，标准本征值问题
- Plan B（放弃）：Galerkin方法+广义本征值问题（$Hc=ESc$）

**选择理由**：
- Plan A在保持谱精度的同时强制边界条件
- Plan B需构造重叠矩阵$S$，增加复杂度
- Plan A与B-样条法形成清晰对比（标准vs广义本征值）

### 决策3：稳定化方法参数选择

**L扫描范围**：[0.5, 3.0]，步长0.05 → 51个L值

**理由**：
- $L<0.5$：盒子过小，共振态受边界干扰
- $L>3.0$：连续态过于密集，跟踪困难
- 步长0.05：平衡分辨率与计算成本

**能量窗口**：[0.9, 1.2] $mc^2$

**理由**：
- 避开$mc^2$以下的束缚态（28个物理态）
- 聚焦正能连续区的共振结构

## 缺陷修复历程

### Defect 1：$r_{max}$过小导致的假通过

**现象**：能量误差<1e-6但波函数在$r=1.0$ a.u.处截断

**根因**：$L=0.1, \alpha=0.2$ → $r_{max}=1.0$ a.u.，铀原子1s态需>7 a.u.才能衰减

**修复**：$L=1.0, \alpha=0.1$ → $r_{max}=20$ a.u.

**验证**：$P(r_{max})=6.40 \times 10^{-06} \ll 0.01$ ✅

### Defect 2：非厄密哈密顿矩阵

**现象**：40%本征值因虚部>1e-8被丢弃

**根因**：Legendre-Gauss节点+直接构造导数矩阵 → 非对称

**修复**：CGL节点+对称化（历史方案）→ 最终改为`eig+.real`

**验证**：296个本征值全部实数保留 ✅

### Defect 3：原点无边界条件

**现象**：波函数在原点处垂直跳变

**根因**：开区间节点不含$r=0$，无法强制$P(0)=0$

**修复**：CGL节点含端点，矩阵截断排除$r=0$和$r=r_{max}$

**验证**：$r_{min}=1.06 \times 10^{-04} > 0$ ✅

## 使用说明

### 运行完整稳定化分析

```bash
python run_stab.py
```

输出：
- `fig_stabilization.png` — 稳定化图（灰色连续态+红色共振平台）
- `fig_resonance_wf.png` — 共振波函数与有效势

### 运行测试套件

```bash
pytest test_solvers.py test_stabilization.py -q
```

预期：16 tests passed

### 运行基础对比分析

```bash
python main.py
```

输出：MAB-GPS与B-样条法的能谱与波函数对比

## 待考证/未知项

以下信息在当前工作目录中**未找到明确记录**，标记为待考证：

1. **B-样条法的具体阶数**（如三次B-样条）——代码中未明确标注
2. **Galerkin Plan B的详细实现方案** ——仅存在于issue_report.md的建议中，未实际编码
3. **初始网格参数的历史演变** ——$N=200 \to 150$的具体测试数据对比
4. **不同屏蔽参数$\lambda$的系统性测试结果** ——仅测试了$\lambda=0.1$
5. **其他核电荷数$Z$的验证** ——仅测试了$Z=92$（铀）
6. **共振宽度的计算方法** ——当前仅识别共振能量，未提取宽度$\Gamma$
7. **计算耗时随$N$的标度律** ——缺乏系统性性能测试数据

## 版本记录

- **v1.0 (原始版)**：Legendre-Gauss节点，非厄密H，虚部过滤
- **v2.0 (修复版)**：CGL节点，对称化+`eigh`，边界截断
- **v3.0 (当前版)**：CGL节点，`eig+.real`，无奇偶解耦
- **稳定化方法**：新增共振态扫描与识别功能

## 参考文档

- `issue_report.md` — 缺陷分析与修复建议（英文）
- `final_rport.md` — 项目总结报告（中文）
- `H_issue.md` — 奇偶解耦bug报告
- `.sisyphus/evidence/` — 验证日志与测试证据
- `.sisyphus/notepads/mabgps-fix/learnings.md` — 技术细节记录

---

**文档生成时间**：2026年3月26日  
**基于代码版本**：stabilization plan完整实现版  
**验证状态**：全部16项测试PASS，物理指标达标
